# HANDOFF — Sesión 2026-07-13 (Mañana)

**Repo:** `C:\Users\Paola\ZCodeProject\github-ai-assistant` · rama `main`  
**Versión actual:** v3.33.8 (commit `1ba5450`, tag `v3.33.8`)  
**Deploy:** Cloud Run OK — `https://github-ai-assistant-748914382449.us-central1.run.app`

---

## 📋 Lectura obligatoria antes de empezar

1. **`CLAUDE.md`** — Guía técnica completa (arquitectura, convenciones, trampas §5)
2. **`METODOLOGIA_IA.md`** — Flujo de trabajo humano↔IA, trazabilidad, lecciones aprendidas

> **Regla de oro:** No tocar nada sin haber leído ambos. Saltárselo es la causa #1 de trabajar en bucle.

---

## 🐛 Contexto del problema (de ayer)

**Cloudflare Workers AI — Cuota de "Neuronas" agotada (10,000/día)**
- Errores en consola: `429` (rate limit) + `403 Forbidden` + `ERR_CONTENT_DECODING_FAILED`
- Causa: Tu cuenta Cloudflare/Z.ai superó la cuota diaria. Modelos GLM 5.2 y DeepSeek R1 Distill consumen más.
- **Solución inmediata:** Mañana se reinicia la cuota. Modelos "baratos" que funcionarán: Kimi K2.7 Code, Llama 3.1 8B, Mistral 7B, Qwen 2.5 7B, Gemma 2 9B.

**Fixes técnicos pendientes (baja prioridad, documentados en CLAUDE.md §5):**
- `client/src/utils/retry.ts`: Añadir `429` a `TRANSIENT_STATUS` y patrón `"rate limit|429"` a `TRANSIENT_PATTERN`
- `server/index.js` proxy `/api/cloudflare`: Strip header `content-encoding` del upstream (evita `ERR_CONTENT_DECODING_FAILED`)
- UI: Mensaje accionable si Cloudflare devuelve 403/429 → *"Modelo X no disponible en tu cuenta. Prueba con Kimi K2.7 Code o Llama 3.1 8B"*

---

## ✅ Lo que SÍ hay que hacer mañana (PRIORIDAD)

### 1. Integrar proveedor **Ollama Cloud** en github-ai-assistant

**Configuración verificada hoy (solo 11 modelos que FUNCIONAN en free tier):**

#### 🟢 **6 modelos 100% free y funcionando**
| Modelo (value) | Label | Contexto | Notas |
|---|---|---|---|
| `minimax-m3` | MiniMax M3 | 1M | **Default recomendado** (tú lo usas, free, 1M ctx) |
| `nemotron-3-super` | Nemotron 3 Super | 1M | Funciona, emite reasoning |
| `qwen3-coder-next` | Qwen3 Coder Next | 1M | Bueno para código |
| `gemma4:31b` | Gemma 4 31B | 262K | Funciona |
| `gpt-oss:20b` | GPT-OSS 20B | 131K | Funciona, emite reasoning |
| `ministral-3:14b` | Ministral 3 14B | 262K | Funciona |

#### ⚠️ **5 modelos free pero con límite de sesión muy bajo** (se agotan en pocos requests)
| Modelo | Label | Contexto | Error típico tras ~3-5 requests |
|---|---|---|---|
| `nemotron-3-ultra` | Nemotron 3 Ultra | 1M | `"reached your session usage limit"` |
| `devstral-small-2:24b` | Devstral Small 2 24B | 1M | `"reached your session usage limit"` |
| `gpt-oss:120b` | GPT-OSS 120B | 131K | `"reached your session usage limit"` |
| `qwen3-coder:480b` | Qwen3 Coder 480B | 1M | `"reached your session usage limit"` |
| `devstral-2:123b` | Devstral 2 123B | 1M | `"reached your session usage limit"` |

#### 🔴 **Modelos que REQUIEREN suscripción (pago) — NO incluir**
`kimi-k2.7-code`, `glm-5.2`, `deepseek-v4-flash`, `mistral-large-3:675b` → devuelven `"this model requires a subscription"`

---

### 2. Archivos a modificar en github-ai-assistant

#### A. `client/src/services/providers.ts`
```typescript
// Añadir al final de PROVIDERS (antes del export), siguiendo patrón de openzen/zenmux:

// Fallback curado con los 11 modelos verificados hoy
const OLLAMA_FALLBACK: ModelOption[] = [
  // Free tier ilimitado (relativamente)
  { value: 'minimax-m3', label: 'MiniMax M3', free: true, recommended: true },
  { value: 'nemotron-3-super', label: 'Nemotron 3 Super', free: true },
  { value: 'qwen3-coder-next', label: 'Qwen3 Coder Next', free: true },
  { value: 'gemma4:31b', label: 'Gemma 4 31B', free: true },
  { value: 'gpt-oss:20b', label: 'GPT-OSS 20B', free: true },
  { value: 'ministral-3:14b', label: 'Ministral 3 14B', free: true },
  // Free pero con límite de sesión bajo
  { value: 'nemotron-3-ultra', label: 'Nemotron 3 Ultra', free: true },
  { value: 'devstral-small-2:24b', label: 'Devstral Small 2 24B', free: true },
  { value: 'gpt-oss:120b', label: 'GPT-OSS 120B', free: true },
  { value: 'qwen3-coder:480b', label: 'Qwen3 Coder 480B', free: true },
  { value: 'devstral-2:123b', label: 'Devstral 2 123B', free: true },
];

ollama: {
  id: 'ollama',
  name: 'Ollama Cloud',
  shortName: 'Ollama',
  emoji: '🦙',
  cardDesc: 'provider.ollama.cardDesc',
  transport: 'openai-compatible',
  chatEndpoint: 'https://ollama.com/v1/chat/completions',
  modelsEndpoint: 'https://ollama.com/v1/models',
  modelsNeedKey: true,
  staticModels: OLLAMA_FALLBACK,
  defaultModel: OLLAMA_FALLBACK[0].value, // 'minimax-m3'
  keyPlaceholder: 'sk-ollama-...',
  keyPrefix: 'sk-ollama-',
  signupUrl: 'https://ollama.com',
  signupLabel: 'provider.ollama.signupLabel',
  note: 'provider.ollama.note',
},
```

#### B. `client/src/i18n/es.ts` + `en.ts`
```typescript
// En el objeto provider:
ollama: {
  cardDesc: 'Modelos abiertos alojados en Ollama Cloud. Requiere cuenta gratuita en ollama.com.',
  signupLabel: 'Crear cuenta en Ollama',
  note: 'Solo modelos gratuitos verificados. Algunos tienen límite de sesión bajo (pocos requests/hora).',
},
```

#### C. **Aparece automáticamente en el panel** (`AIProviderPanel.tsx`) al estar en `PROVIDERS` — no tocar UI.

---

### 3. Test rápido mañana
1. `npm run dev` en raíz
2. Abrir panel de proveedores → seleccionar **🦙 Ollama Cloud**
3. Pegar tu API key: `d080dff5e65647d8bc7980a87ba2361c.VOWGY1eSjXc1c41nn-oo5l0N` (o la que uses)
4. Seleccionar **MiniMax M3** (default) → Conectar
5. Probar chat simple: "Hola, ¿qué modelo eres?" → Debe responder

---

## 🔧 Fixes técnicos menores (opcional, si hay tiempo)

| Archivo | Qué hacer |
|---|---|
| `client/src/utils/retry.ts:9` | `const TRANSIENT_STATUS = new Set([429, 500, 502, 503, 504]);` |
| `client/src/utils/retry.ts:11` | Añadir `\|rate limit\|429` a `TRANSIENT_PATTERN` |
| `server/index.js:435-440` | En proxy Cloudflare, antes de `pipeUpstream`: `upstream.headers.delete('content-encoding');` |
| `client/src/services/gemini.ts:253-258` | En `callOpenAICompatible`, si `res.status === 403` y proveedor === 'cloudflare', error message accionable |

---

## 📦 ZCode config actualizado (ya hecho)

**Archivo:** `C:\Users\Paola\.zcode\v2\config.json`  
**Provider:** `b4fb2357-54b6-4a7e-8e99-e828bb849080` (Ollama Cloud)  
**Modelos:** **Solo los 11 verificados** (6 free ilimitados + 5 free con límite de sesión)  
**Eliminados:** 29 modelos que no funcionan (requieren suscripción, no existen en cloud, o son duplicados)

---

## 🎯 Checklist de arranque mañana

- [ ] Leer `CLAUDE.md` y `METODOLOGIA_IA.md` completos
- [ ] Verificar que cuota Cloudflare se reinició (probar Kimi K2.7 Code en app)
- [ ] Añadir proveedor `ollama` en `providers.ts` + claves i18n
- [ ] `npm run build` → `npm run test:run` → `npm run lint` (todo verde)
- [ ] Probar conexión Ollama Cloud con MiniMax M3 en UI
- [ ] Commit + push (rutina automática: bump → changelog → tag → release → deploy)

---

## 💡 Notas importantes

- **NO usar Ollama Local** (`http://localhost:11434`) — solo Ollama Cloud (`https://ollama.com/v1`)
- **Zero-Storage:** La API key de Ollama vive solo en `AIProviderContext` (React state), nunca en localStorage
- **Catálogo dinámico:** `modelsEndpoint` existe → `fetchModels` en `gemini.ts` ya lo soporta para `openai-compatible` (líneas 555-577). Si la key es válida, cargará modelos dinámicos; si falla, usa `OLLAMA_FALLBACK`.
- **Modelos con `:` en el nombre** (`gemma4:31b`, `qwen3-coder:480b`) → el parser actual lo maneja bien (value = id completo).

---

**¿Listo para empezar? ¡A por ello!** 🚀