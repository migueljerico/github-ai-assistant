fix(docs): README footer now uses Markdown link and full attribution text

In gemini.ts generateRepoDocs(), the docFooter variable was generating
the author as plain text '@login' and omitting the app attribution.

Fixed to match buildSignature() output in assistantActions.ts:
- @login → [@login](https://github.com/login) (clickable Markdown link)
- Added 'desde la App Asistente de IA' (ES) / 'from the AI Assistant App' (EN)
- Removed the '· year' suffix (year was only in the footer, not in About)

Now the README footer generated on 'Documentar repo' is consistent with
the repo About description set by updateRepoAbout().
