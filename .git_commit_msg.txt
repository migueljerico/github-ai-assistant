fix(docs): README footer link and attribution text in generateRepoDocs

gemini.ts docFooter was generating @login as plain text and missing
the 'desde la App Asistente de IA' attribution. Year was kept.

Changes in gemini.ts lines 798-800:
- @owner plain text -> <a href="https://github.com/owner">@owner</a>
- Added 'desde la App Asistente de IA' (ES) / 'from the AI Assistant App' (EN)
- Year (· docYear) preserved at end

Updated gemini.test.ts line 903 to match new footer format.

Tests: 93/93 gemini tests green. Build clean.
